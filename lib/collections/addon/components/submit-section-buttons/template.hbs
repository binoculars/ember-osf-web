{{#if showDiscard}}
    <button class="btn btn-default" {{action discard}}>
        {{t (concat 'collections.submit_section.discard')}}
    </button>
{{/if}}
<button class="btn btn-primary" {{action continue}} disabled={{continueDisabled}}>
    {{t (concat 'collections.submit_section.save')}}
</button>